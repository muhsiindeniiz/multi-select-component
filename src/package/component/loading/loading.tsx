import React, { FC } from "react";
import { LoadingProps } from "./loading.type";

const Loading: FC<LoadingProps> = ({ message, height }) => {
  return <div>Loading...</div>;
};

export default Loading;
